@model List<WebMVC.Models.OperationClaimModel>
@{
    ViewData["Title"] = "OperationClaims";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="card">
    <h5 class="card-header">Operation Table</h5>
    <div class="table-responsive text-nowrap">
    <a href="/Operation/Add" class="badge bg-label-primary me-2" style="margin-left:10px;">Yeni Operasyon Ekle</a>
        <table class="table">
            <thead>
                <tr>
                    <th>Operation Id</th>
                    <th>Operation Name</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.OperationId</td>
                        <td><i class="fab fa-angular fa-lg text-danger me-3"></i> <strong>@item.OperationName</strong></td>

                        <td>
                            <div class="dropdown">
                                <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                    <i class="bx bx-dots-vertical-rounded"></i>
                                </button>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" href="/Operation/Update/@item.OperationId"><i class="bx bx-edit-alt me-1"></i> Edit</a>
                                    <a class="dropdown-item" href="/Operation/Remove/@item.OperationId"><i class="bx bx-trash me-1"></i> Delete</a>
                                </div>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
